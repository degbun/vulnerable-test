[tox]
envlist = py39

[testenv]
deps =
    pytest
    coverage
commands =
    coverage run -m pytest
    coverage xml
    coverage report

[coverage:run]
relative_files = True
source = .
branch = True
